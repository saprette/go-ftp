package main

import (
	"fmt"
	"github.com/unloadedroe/filesystem"
)

/*
GOPATH=GoFTP/goftp

Run test:
	> go install github.com/unloadedroe/server
	> ./bin/server
*/

func main() {
	//path := filesystem.NewPath("hello.txt")
	//fmt.Println(string(filesystem.Read(path).Content))
}
